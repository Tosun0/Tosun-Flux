<Window x:Class="TosunFlux.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tosun Flux"
        Icon="pack://application:,,,/Assets/Tosun.ico"
        FontFamily="pack://application:,,,/Assets/#Pretendard"
        FontWeight="Normal"
        Width="1160" Height="900" MinWidth="980" MinHeight="880"
        WindowStartupLocation="CenterScreen"
        Background="Transparent" AllowsTransparency="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        AllowDrop="True" DragOver="Window_DragOver" Drop="Window_Drop">
    <Window.Resources>
        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource GlassBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius" Value="28" />
            <Setter Property="Padding" Value="22" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="8" Opacity="0.35" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="18,11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="16" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.38" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource FieldBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="13,10" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource FieldBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MaxDropDownHeight" Value="260" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="DropDownToggle" Focusable="False" Foreground="{TemplateBinding Foreground}" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderThickness="0" CornerRadius="15">
                                            <Grid Margin="13,0,13,0">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="22" /></Grid.ColumnDefinitions>
                                                <ContentPresenter VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                                                <Path Grid.Column="1" Data="M 1 1 L 6 6 L 11 1" Stroke="{StaticResource ArrowBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                                <Border Margin="0,7,0,0" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}" Padding="7" CornerRadius="16" Background="{StaticResource PopupBrush}" BorderThickness="0">
                                    <Border.Effect><DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="7" Opacity="0.5" /></Border.Effect>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"><ItemsPresenter /></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="11,9" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemSurface" CornerRadius="10" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemSurface" Property="Background" Value="{StaticResource HoverBrush}" /></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemSurface" Property="Background" Value="{StaticResource SelectedBrush}" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FitChoice" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="13" Padding="10,9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="{StaticResource AccentBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Opacity" Value="0.82" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Surface" Property="Opacity" Value="0.38" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource StrongGlassBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18" Padding="{TemplateBinding Padding}" ClipToBounds="True">
                            <ScrollViewer Focusable="False" Padding="0"><ItemsPresenter /></ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="9" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton><RepeatButton Opacity="0" Command="ScrollBar.PageUpCommand" /></Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{StaticResource ScrollThumbBrush}" CornerRadius="5" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton><RepeatButton Opacity="0" Command="ScrollBar.PageDownCommand" /></Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource RootOverlayBrush}">
        <Ellipse Width="620" Height="620" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-260,-300,0,0" IsHitTestVisible="False">
            <Ellipse.Fill><RadialGradientBrush><GradientStop Color="#5A596CC8" Offset="0" /><GradientStop Color="#00596CC8" Offset="1" /></RadialGradientBrush></Ellipse.Fill>
        </Ellipse>
        <Ellipse Width="720" Height="720" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-320,-390" IsHitTestVisible="False">
            <Ellipse.Fill><RadialGradientBrush><GradientStop Color="#52685DBD" Offset="0" /><GradientStop Color="#00685DBD" Offset="1" /></RadialGradientBrush></Ellipse.Fill>
        </Ellipse>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="96" />
                <RowDefinition Height="12" />
                <RowDefinition Height="*" />
                <RowDefinition Height="12" />
                <RowDefinition Height="120" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="160" /></Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="TOSUN FLUX" FontSize="34" FontWeight="Black" Foreground="#FFFFFFFF" />
                    <TextBlock Text="토순의 파일 컨버터  ·  파일을 넣고 형식을 골라 변환하세요" Margin="2,8,0,0" FontSize="14" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="CheckUpdateButton" Content="업데이트 확인" Padding="14,9" Background="{StaticResource SecondaryBrush}" Foreground="{StaticResource SecondaryTextBrush}" Click="CheckUpdateButton_Click" />
                    <Button x:Name="UpdateButton" Visibility="Collapsed" Content="업데이트" Padding="14,9" Margin="8,0,12,0" Click="UpdateButton_Click" />
                    <Button x:Name="HelpButton" Content="?" Width="34" Height="34" Margin="20,0,0,0" Padding="0" FontSize="17" FontWeight="Black" Background="{StaticResource SecondaryBrush}" Foreground="{StaticResource SecondaryTextBrush}" ToolTip="변환 형식 도움말" Click="HelpButton_Click" />
                </StackPanel>
                <Grid Grid.Column="2" Width="160" Height="96" HorizontalAlignment="Center" VerticalAlignment="Center" Panel.ZIndex="2">
                    <Image x:Name="TosunMascot" Source="pack://application:,,,/Assets/tosun-floating-v1.png" Height="92" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="Hand" ToolTip="토순이를 눌러 보세요" MouseLeftButtonUp="TosunMascot_Click" />
                    <Border x:Name="TosunSpeechBubble" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-8,-8,0" Padding="12,7" Background="{StaticResource StrongGlassBrush}" CornerRadius="15" Panel.ZIndex="1">
                        <Border.Effect><DropShadowEffect Color="#000000" BlurRadius="14" ShadowDepth="4" Opacity="0.35" /></Border.Effect>
                        <TextBlock Text="토순토순" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    </Border>
                </Grid>
                <TextBlock Grid.ColumnSpan="3" Text="dev v1.1.0." HorizontalAlignment="Right" VerticalAlignment="Top" TextAlignment="Right" Margin="0,2,4,0" FontSize="10" FontWeight="Light" Foreground="{StaticResource MutedBrush}" IsHitTestVisible="False" Panel.ZIndex="1" />
            </Grid>

            <Grid x:Name="WorkspaceGrid" Grid.Row="2">
                <Grid.ColumnDefinitions><ColumnDefinition Width="1.55*" /><ColumnDefinition Width="16" /><ColumnDefinition Width="1*" /></Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource GlassCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="14" /><RowDefinition Height="86" /><RowDefinition Height="12" /><RowDefinition Height="Auto" /><RowDefinition Height="10" /><RowDefinition Height="*" /><RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="변환할 파일" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                        <Border Grid.Row="2" CornerRadius="20" Background="{StaticResource DropSurfaceBrush}" BorderThickness="0" MouseLeftButtonUp="DropSurface_Click">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="파일을 여기에 끌어다 놓으세요" HorizontalAlignment="Center" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                                <TextBlock Text="또는 클릭해서 여러 파일을 선택합니다" HorizontalAlignment="Center" Margin="0,7,0,0" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                            </StackPanel>
                        </Border>
                        <StackPanel Grid.Row="4" Orientation="Horizontal">
                            <Button Content="파일 추가" Click="AddFiles_Click" />
                            <Button Content="전체 비우기" Click="ClearFiles_Click" Margin="9,0,0,0" Background="{StaticResource SecondaryBrush}" Foreground="{StaticResource SecondaryTextBrush}" />
                        </StackPanel>
                        <ListBox x:Name="FilesList" Grid.Row="6" Background="{StaticResource StrongGlassBrush}" BorderThickness="0" Padding="10" Foreground="{StaticResource TextBrush}" FontSize="13" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="2,2,2,4">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="34" /></Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                        <Button Grid.Column="1" Content="×" Width="30" Height="30" Padding="0" FontSize="17" HorizontalAlignment="Right" VerticalAlignment="Center" Background="{StaticResource SecondaryBrush}" Foreground="{StaticResource SecondaryTextBrush}" Tag="{Binding}" Click="RemoveFile_Click" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock x:Name="FileHint" Grid.Row="7" Text="지원 형식은 파일을 추가하면 자동으로 안내됩니다." Margin="2,10,0,0" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource GlassCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="6" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="6" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="6" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="6" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="변환 설정" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                        <Grid x:Name="OutputLabelsGrid" Grid.Row="2">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="8" /><ColumnDefinition Width="*" /><ColumnDefinition Width="8" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Text="출력 형식" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Column="2" Text="최적화" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock x:Name="FrameRateLabel" Grid.Column="4" Text="프레임 변환" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                        </Grid>
                        <Grid x:Name="OutputControlsGrid" Grid.Row="4">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="8" /><ColumnDefinition Width="*" /><ColumnDefinition Width="8" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <Border CornerRadius="15" ClipToBounds="True"><ComboBox x:Name="TargetBox" MinHeight="42" SelectionChanged="TargetBox_SelectionChanged" /></Border>
                            <Border Grid.Column="2" CornerRadius="15" ClipToBounds="True"><ComboBox x:Name="OptimizationBox" MinHeight="42" SelectionChanged="OptimizationBox_SelectionChanged" /></Border>
                            <Border x:Name="FrameRatePanel" Grid.Column="4" CornerRadius="15" ClipToBounds="True"><ComboBox x:Name="FrameRateBox" MinHeight="42" SelectionChanged="FrameRateBox_SelectionChanged" /></Border>
                        </Grid>
                        <Grid Grid.Row="6">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Text="해상도" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Column="2" Text="화면비" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                        </Grid>
                        <Grid Grid.Row="8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <Border CornerRadius="15" ClipToBounds="True"><ComboBox x:Name="ResolutionBox" MinHeight="42" SelectionChanged="ResolutionBox_SelectionChanged" /></Border>
                            <Border Grid.Column="2" CornerRadius="15" ClipToBounds="True"><ComboBox x:Name="AspectBox" MinHeight="42" SelectionChanged="AspectBox_SelectionChanged" /></Border>
                        </Grid>
                        <Grid x:Name="CustomSizePanel" Grid.Row="9" Visibility="Collapsed" Margin="0,4,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="30" /><ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border CornerRadius="15" ClipToBounds="True"><TextBox x:Name="CustomWidthBox" Text="1920" MaxLength="5" MinHeight="38" VerticalContentAlignment="Center" ToolTip="가로 픽셀" TextChanged="CustomSizeBox_TextChanged" /></Border>
                            <TextBlock Grid.Column="1" Text="×" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" FontSize="14" />
                            <Border Grid.Column="2" CornerRadius="15" ClipToBounds="True"><TextBox x:Name="CustomHeightBox" Text="1080" MaxLength="5" MinHeight="38" VerticalContentAlignment="Center" ToolTip="세로 픽셀" TextChanged="CustomSizeBox_TextChanged" /></Border>
                        </Grid>
                        <TextBlock Grid.Row="10" Text="화면 맞춤" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                        <Grid Grid.Row="12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="7" /><ColumnDefinition Width="*" /><ColumnDefinition Width="7" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <RadioButton x:Name="FitChoice" GroupName="FitMode" Content="맞추기" IsChecked="True" Style="{StaticResource FitChoice}" />
                            <RadioButton x:Name="FillChoice" Grid.Column="2" GroupName="FitMode" Content="채우기" Style="{StaticResource FitChoice}" />
                            <RadioButton x:Name="StretchChoice" Grid.Column="4" GroupName="FitMode" Content="늘리기" Style="{StaticResource FitChoice}" />
                        </Grid>
                        <TextBlock Grid.Row="14" Text="저장 위치" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                        <Grid Grid.Row="16">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <Border CornerRadius="15" ClipToBounds="True"><TextBox x:Name="OutputPath" MinHeight="42" VerticalContentAlignment="Center" /></Border>
                            <Button Grid.Column="1" Content="폴더 선택" Click="ChooseFolder_Click" Margin="8,0,0,0" />
                        </Grid>
                        <TextBlock x:Name="OptimizationHint" Grid.Row="17" Text="이미지 · 영상 · PDF에 적용됩니다." Margin="2,8,0,0" FontSize="12" FontWeight="Light" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="OriginalSizeText" Grid.Row="18" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="2,0,2,8" FontSize="11" FontWeight="Light" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" />
                        <TextBlock x:Name="EstimatedSizeText" Grid.Row="18" Text="예상 용량은 파일을 추가하면 표시됩니다." HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="2,0,2,8" FontSize="11" FontWeight="Light" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" ToolTip="파일별 해상도·프레임과 형식별 압축 편차를 반영한 예상 범위입니다." />
                        <Button x:Name="ConvertButton" Grid.Row="19" Content="변환 시작" Height="50" IsEnabled="False" Click="Convert_Click" />
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="4" Style="{StaticResource GlassCard}" Padding="20,16">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="7" /><RowDefinition Height="8" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <TextBlock x:Name="StatusText" Text="파일을 추가해 주세요." Foreground="{StaticResource MutedBrush}" FontWeight="Light" FontSize="12" />
                        <TextBlock Text="© 2026 Tosun Studio. All rights reserved." HorizontalAlignment="Right" Foreground="{StaticResource MutedBrush}" FontWeight="Light" FontSize="10" />
                    </Grid>
                    <ProgressBar x:Name="Progress" Grid.Row="2" Minimum="0" Maximum="1" Height="7" Background="{StaticResource ProgressTrackBrush}" Foreground="{StaticResource AccentBrush}" BorderThickness="0" />
                    <TextBox x:Name="LogText" Grid.Row="4" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="{StaticResource LogBrush}" BorderThickness="0" FontSize="12" Padding="10" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
